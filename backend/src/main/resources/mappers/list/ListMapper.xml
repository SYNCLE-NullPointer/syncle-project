<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nullpointer.domain.list.mapper.ListMapper">

    <!-- 리스트 1개 INSERT -->
    <insert id="insertList"
            parameterType="com.nullpointer.domain.list.vo.ListVo"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO list (board_id, title, order_index, created_at, updated_at, deleted_at)
        VALUES (#{boardId}, #{title}, 0, NOW(), NOW(), NULL)
    </insert>

    <!--
      리스트 목록 조회
      조건:
        - 해당 board_id
        - deleted_at IS NULL (삭제 안 된 것만)
        - order_index 오름차순 정렬
    -->
    <select id="findByBoardId"
            parameterType="long"
            resultType="com.nullpointer.domain.list.vo.ListVo">
        SELECT
            id,
            board_id,
            title,
            order_index AS orderIndex
        FROM list
        WHERE board_id = #{boardId}
          AND deleted_at IS NULL
        ORDER BY order_index ASC
    </select>

</mapper>
